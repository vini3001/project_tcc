import { globalCss } from "@stitches/react";


export const globalStyles = globalCss({
    '*': {
        margin: 0,
        padding: 0,
        fontFamily: "Roboto, sans-serif",
        fontWeight: 400,
        fontStyle: 'normal'
    },
})