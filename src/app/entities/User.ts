export type User = {
    id: string
    name: string
    email: string
    phone?: string
    password: string
    cpf?: string
    status: string
    rg?: string
    avatarUrl?: string
    createdAt?: Date
}